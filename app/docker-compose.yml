version: "3.9"

services:
  penguin-detector:
    build: .
    ports:
      - "8501:8501"
    environment:
      - YOLO_CONFIG_DIR=/tmp
    volumes:
      - .:/app
    mem_limit: 8g
    memswap_limit: 8g
